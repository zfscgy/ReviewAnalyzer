This is a simple application of the Bertopic model.

Bertopic takes multiple documents (list of string) as input, then clusters them using the embeddings generated by the BERT model and cluster them into multiple topics.